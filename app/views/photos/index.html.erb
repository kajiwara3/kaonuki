<div data-role="header" data-theme="b" >
  <h1>Kaonuki</h1>
</div>
 <ul data-role="listview">
  <% @photos.each do |photo| %>
  <li>
    <%= link_to image_tag(photo.image_url(:thumb).to_s) + photo.title, photo %>
  </li>
  <% end %>
 </ul>
<br />

<%= link_to 'New Photo', new_photo_path %>
